# 商队（Caravan）Mod 更新日志

## 0.8.9
- 领袖等待机制：`guardsBlockMovement` 检查从仅 AWAY 模拟阶段扩展到【出发步行（DEPART）】与【回归步行（RETURN）】阶段——卫兵战斗时领袖在任意移动阶段都会停等。
- 战斗判定增强：除 AI 状态 `ATTACKING` 外，卫兵最近 3 秒内攻击过/受到攻击也视为战斗中（捕捉状态机切换间隙的短暂战斗，避免漏检）。
- 诊断：驻守期间存在威胁或处于战斗中时，同样输出“Caravan: 护卫卫兵……”状态日志（节流 200 刻）。

## 0.8.8
- 状态保存修复：卫兵塔【商队护卫】模式改为在构造时写入设置内部列表（原为 getSettings() 副本）——StringSetting 按索引保存，加载时 updateSetting 会把越界索引收敛到 size-1（巡逻矿井），导致重进游戏后模式跳回【巡逻矿井】。
- 索敌修复：敌对判定与 Minecolonies 原版对齐（怪物列表 且 不在卫兵塔【敌对】豁免名单）——此前“在名单=敌对”逻辑相反，关闭史莱姆索敌后仍会战斗；同时消除由此产生的威胁表虚目标。
- 领袖等待修复：`guardsBlockMovement` 改为以卫兵 AI 状态（ATTACKING）判定战斗，不再以“威胁表非空”判定——豁免怪物的虚威胁不再触发“护卫卫兵战斗中”等待。
- 商队护卫不再随机入睡（含驻守期间），避免睡眠期不索敌。
- 诊断：卫兵状态日志新增“原版可攻击”字段（该威胁目标是否会被原版战斗判定接受）。

## 0.8.7
- AI 修复（跟随索敌）：追击参考点从“卫兵塔”改为“卫兵自身位置”——卫兵跟随商队远离卫兵塔后，KnightCombatAI 的追击范围判定不再拒绝塔外的敌人；商队模式下禁止随机入睡（防止睡眠期卡住不索敌）；缺少武器时不再卡死在 PREPARING——保持 ATTACKING 追击并以异步公民请求补发武器；卫兵与领袖距离超过 30 格时直接传送跟随（原版 TeleportHelper 在模拟消失点常失败）。
- 保护机制：商队领袖/成员/护卫卫兵在模拟旅行（隐形）期间改为无敌并清空威胁表与最近攻击者，回归/穿越现身时恢复——mob 不再能攻击隐形中的商队人员。
- 兼容（pathfinding edition）：在语言文件中补充 `read_mind.state.NO_TARGET/ATTACKING` 及商队自定义状态（备货/赶路/交易/返回小屋/外出/回归/跟随领袖/准备消失/消失）的本地化。
- 诊断：卫兵状态日志增加 AI 状态、是否持有武器、当前威胁目标。
- 崩溃修复：`AbstractEntityAIGuardMixin` 不再 `@Shadow` 父类声明的方法（Mixin 无法在目标类解析），武器补发改为经 redirect 参数缓存卫兵实体后调用 `CitizenData.createRequestAsync`。

## 0.8.6
- AI 修复：商队出行但未进入模拟旅行时，护卫卫兵改为跟随领袖（此前错误地驻守小屋）；战斗判定改用威胁表目标（卫兵战斗可能不设置 getTarget）；新增卫兵状态诊断日志（节流）。
- 机制：模拟旅行中护卫卫兵视同商队成员——纳入喂食与帐篷耐久消耗人数。

## 0.8.5
- 机制（商队护卫·回归/穿越同步）：模拟旅行结束（回归）或穿越殖民地时，卫兵传送到领袖旁并现身/隐形同步（与领袖/成员一起）。
- 机制（装备隐藏）：卫兵消失时护甲/主手武器暂存背包，回归时穿回（连同装备一起隐藏）。
- AI：护卫卫兵跟随/驻守时同时索敌（扫描 16 格敌对加入威胁表，触发卫兵自身战斗）；商队领袖在护卫卫兵战斗时停止移动，战斗结束后等待卫兵回到 6 格内再继续。

## 0.8.4
- GUI：商队小屋【护卫】页重构为参照矿井【守卫指派】页（layoutguardlist.xml）——每行显示卫兵名字，按钮样式/文字“分配/取消分配”复用本体键；列表按卫兵输出（名字/所属塔/指派状态）。
- AI：新增卫兵消失逻辑（`AbstractAISkeletonMixin`）——商队模拟旅行时卫兵寻路到领袖消失位置并一同隐形，消失期间冻结 job AI（不索敌/不觅食/不移动），模拟结束自动现身恢复驻守/跟随。

## 0.8.3
- 修复（卫兵塔工作模式无【商队护卫】）：改为在 `StringSetting.getSettings()` 动态追加选项（覆盖已从 NBT 反序列化的旧卫兵塔设置，不再依赖构造注入）；输出一次诊断日志。
- GUI：商队小屋【护卫】页图标改为【矿井】-【守卫指派】页图标（`minecolonies:textures/gui/modules/sword.png`）。
- 诊断：卫兵 AI 判定【商队护卫】模式时输出一次日志（含是否被商队小屋选中）。

## 0.8.2
- 修复崩溃（mixin）：`AbstractEntityAIGuardMixin` 不再 `@Shadow` 父类字段（mixin 无法在目标类解析继承字段）——只 shadow 目标类自身的 `buildingGuards`；指派改为【塔级】（`CaravanGuardModule` 持久化被选中的卫兵塔位置，AI 按卫兵塔位置判定），无需访问卫兵实体字段。

## 0.8.1
- 修复崩溃（启动加载失败）：`AbstractEntityAIGuardMixin` 的 `@Shadow building` 字段类型错误（父类声明为 `AbstractBuilding`），导致 mixin `attachFields` 失败、Mod 进入 broken 状态。

## 0.8.0
- 重构（商队卫兵 → mixin 方案）：回退 0.7.2-0.7.9 的商队卫兵职业实现，改为复用 Minecolonies 卫兵塔卫兵：
  - 卫兵塔【工作模式】新增【商队护卫】选项（mixin `GuardTaskSetting`）；
  - 商队小屋新增【护卫】页（`CaravanGuardModule` + GUI），列出商队护卫模式的卫兵，选中后开始护卫；
  - 卫兵 AI（mixin `AbstractEntityAIGuard`）：商队领袖消失时跟随到领袖位置，商队未出发时驻守商队小屋，索敌战斗沿用卫兵自身框架。

## 0.7.9
- 修复（卫兵装备领取）：确认本体机制——公民请求（`CitizenData.createRequest`）内部即“工作建筑请求”，快递员送达小屋存储；本体的“领取”由卫兵 AI 从小屋存储把装备转移到背包（`checkForToolOrWeapon`/`equipInventoryArmor`）。卫兵 AI 现增加该领取逻辑（小屋存储 → 背包 → 穿戴）。
- 装备栏顺序说明：反编译确认本体 `ContainerCitizenInventory` 按 MC `EquipmentSlot.values()` 顺序渲染（FEET 在上、HEAD 在下），`getIndex()` 映射 FEET=0…HEAD=3，本体市民与商队卫兵使用同一存储/映射，顺序一致。

## 0.7.8
- 修复（卫兵装备领取）：装备请求改为真正的公民请求（`CitizenData.createRequest`，requester=卫兵市民，快递员直接送达市民背包）——此前 `building.createRequest(guard, …)` 只是“关联公民”的建筑请求，快递员会送到小屋存储导致卫兵永远拿不到装备。

## 0.7.7
- 商队卫兵 AI：新增守卫-跟随-索敌战斗 AI（参照 Minecolonies 骑士/卫兵塔）——商队未出发时驻守小屋（目标=小屋方块），出发后跟随领袖并主动攻击【敌对】选项卡列表/MC 敌对生物（16 格索敌、3 格近战、20 刻攻击冷却），领袖消失时停止战斗并寻路到消失位置一同消失/现身。
- 修复（卫兵不领取装备）：请求送达背包的武器/护甲自动穿戴（护甲入对应槽、武器入主手）。

## 0.7.6
- 调整（卫兵武器请求）：武器（MAINHAND）取消最低等级要求——各等级建筑均可匹配 1-5 级武器（上限仍=建筑等级，5 级建筑可请求 1-5 级剑）。

## 0.7.5
- 修复（卫兵装备等级）：复制 Minecolonies 本体各等级卫兵塔的装备请求配置——护甲按建筑等级 1/2/3/4/5 分别请求等级 1-1/1-2/1-3/2-4/3-∞（5 级含 5 级下界合金），剑等级上限=建筑等级（5 级可请求 5 级武器）；候选/已有装备判定改用 `GuardGear.minArmorLevel/maxArmorLevel`（与本体 `checkForToolOrWeaponAsync` 一致）。

## 0.7.4
- 修复（卫兵装备候选列表）：取消候选物品 12 个上限——多 mod 环境下注册表遍历顺序不定，截断会导致部分 mod 装备无法被请求；现收集该装备类型+等级范围内的全部物品（本体基于类型/tag 匹配、无此限制）。
- 本地化：新增 `com.caravan.gui.guard_equipment`（商队卫兵装备 / Caravan Guard Equipment）。

## 0.7.3
- 修复（卫兵装备请求）：改为沿用 Minecolonies 本体卫兵塔模式——按 `GuardGear`（装备槽位/类型/等级范围/建筑等级范围）检查，缺失时以“该类型+等级范围内的候选物品列表”创建【公民请求】（关联卫兵市民，快递员直接送达市民背包）。
- GUI 修复：饱食度 Tooltip 改用 BlockUI `Tooltip` 控件（`drawSelfLast` 绘制在最上层），解决 onHoverId 隐藏文本不显示、且被物品图标角标遮挡的问题。

## 0.7.2
- GUI：消耗品饱食度图标改为玩家 HUD 鸡腿贴图（minecraft:hud/food_full / food_empty，与 Minecolonies 公民信息页一致），Tooltip 显示饿肚子人数。
- 新职业【商队卫兵】：模型/属性参照骑士（主力量、副耐力），雇佣上限按小屋等级 1/3/5 解锁 1/2/3 人；新增装备请求模块（每殖民地刻检查武器+护甲，按等级请求铁/钻石/下界合金，送达小屋存储）；商队小屋新增【敌对】选项卡（复用本体 EntityListModule）。

## 0.7.1
- 机制调整：消耗品帐篷显示上限 3 顶，不再与饥饿人数挂钩；返回通报加入“X人处于饥饿状态”；模拟移动速度下限 1 格/殖民地刻（防止速度归零无法回归，原有 `Math.max(1, …)` 即满足）。
- 机制调整：商队从 AWAY 回归时，解除隐形的时机后延 1 殖民地刻（回归瞬间保持隐形，由 RETURN 状态第一个 tick 现形）。
- GUI：饱食度图标 Tooltip 显示“商队中没有人饿肚子/商队中有X人饿肚子”；消耗品行重新排布（帐篷3-食物3-饱食度-火把3）。

## 0.7.0
- 新机制（饥饿）：商队人员（领袖/成员）每有一人饱食度为 0（饥饿），商队模拟移动速度 -5%（下限 0）。
- GUI（消耗品）：食物图标右侧新增饱食度图标——无人饥饿显示 MC 原版满饱食度图标（熟鸡腿），有人饥饿显示空饱食度图标（生鸡腿）；每有一人饥饿，帐篷显示数量 -1（下限 0）。
- 清理：删除心跳诊断日志、扎营扣耐久日志、状态变化日志与判定诊断日志（扎营修复已确认生效——心跳无断点、夜间扎营正常）。

## 0.6.38
- 修复（夜间扎营异常·根因）：AWAY 模拟期间每殖民地刻保持领袖隐形（穿越殖民地步行期间除外）——实体重载后 invisible 标志不持久化，丢失后 Minecolonies 睡眠机制会在夜间接管商队领袖的 job AI（心跳中断、夜间不扎营、清晨才补 CAMP）。保持隐形后 `CitizenSleepHandler.shouldGoSleep` 对隐形市民返回 false，睡眠机制不再接管。

## 0.6.37
- 修复（扎营/作息时间错乱）：商队所有昼夜判定（扎营/夜行/醒来、白天开始、出发时间窗、日志模块睡眠/夜行窗口）改用真实 `world.getDayTime()` 而非 `world.getGameTime()`——玩家用原版睡眠跳过夜晚时 `dayTime` 跳变而 `gameTime` 不变，偏差永久累积，导致商队扎营/醒来时刻随偏差漂移（实测偏差 380 刻→睡觉后 7799 刻，扎营推迟到真实凌晨）。游戏日计数与防重仍用 `gameTime`（稳定递增，不受睡觉跳变影响）。

## 0.6.36
- 诊断：`stayAway` 每 1200 刻输出一次 AI 心跳日志（gameTime/dayTime 日刻、阶段、回程距离、模拟状态、away/隐形/睡觉标志），用于确认夜间商队 AI 是否仍被执行（定位“扎营中触发过晚”）。

## 0.6.35
- 诊断：状态变化与每日扎营总结时输出完整时间判定上下文（gameTime 日刻、真实 dayTime 日刻、入睡时刻、日落、WORK_LONGER 研究强度、火把/帐篷数量与耐久、去程阶段、当前状态），用于定位“扎营中触发过晚”（实测稳定在 23673~23688 刻）问题。

## 0.6.34
- 回退 0.6.33 的“交易中阶段触发状态机”修改：交易中阶段保持“交易中”（不触发扎营/露宿/夜行状态机），交易倒计时结束后再单独执行一次 1-3 条检查（恢复 0.6.32 行为）。保留 0.6.33 的其余修改：每日 100 刻恢复“旅行中”、状态日志使用当前游戏日刻数。

## 0.6.33
- 确认并修复（“扎营中”触发过晚）：状态切换与模拟停止移动同处于 `decideCampStatus`（状态设置与停止在同一个 tick），但旧实现中【交易中阶段跳过状态机】——商队夜间到达交易地后保持“交易中”，直到交易倒计时结束（可能已是天亮前数百刻）才转“扎营中”。现交易中阶段仅当处于夜间休息窗口（殖民地工作时间结束或凌晨 < 100 刻）时暂停交易并转入扎营/露宿（不推进交易倒计时），夜行窗口与白天保持交易中。
- 机制：商队切换回“旅行中”的时间调整为每日 100 刻时（0~100 刻凌晨仍为扎营/露宿休息，不再在 0 刻立即恢复移动）。
- 日志：状态变化日志中的刻数改为当前游戏日刻数（`dayTime`，% 24000）而非世界总刻数。

## 0.6.32
- 重构：商队模拟旅行行进与旅行地图联动改为“每殖民地刻状态机”——新增 `JobCaravanLeader.CampStatus`（旅行中/夜行中/扎营中/露宿中/交易中），由商队 AI 按“当前时间 + 商队物品”决策：
  1. 时间 ≥ 殖民地工作时间：停止移动——有帐篷=扎营中，无帐篷=露宿中；
  2. 日落 ≤ 时间 < 工作时间且处于移动：有火把=夜行中（每殖民地刻消耗 1 火把），无火把则按第 1 条扎营/露宿（停止）；
  3. 时间 < 日落：恢复移动（旅行中）——扎营中醒来扣帐篷耐久、露宿中醒来提升患病概率（均每天一次）；
  4. 交易倒计时结束后单独执行一次 1-3 条检查。
- 状态变化输出日志：“当前时间 XXX 刻，商队状态 [YYY]”（世界总刻数）。
- 旅行地图与日志页状态显示改为直接使用服务端同步的 CampStatus（不再由客户端按本地时间推断）；删除旧的“日落/夜行结束/扎营”日志与 wasIn* 标志、sleeping 字段、模块 resting 驱动。

## 0.6.31
- 修复（原版睡眠跳过夜晚后商队卡在“扎营中”）：玩家睡觉把 dayTime 瞬间跳到次日 0 时，商队 AI 可能从未 tick 到“醒来”重置分支（resting 保持 true），地图/日志卡在“扎营中”。现 `CaravanLogModule.onColonyTick` 在睡眠窗口边界变化时直接按时间设置商队 resting（进入=休息、退出=解除）并同步客户端，醒来不再依赖 AI tick。

## 0.6.30
- 清理：删除两处状态转换调试日志（AI 视角与模块视角），保留 `CaravanLogModule.onColonyTick` 的睡眠/夜行窗口边界 `markDirty` 同步（地图“扎营中/露宿中”显示修复的关键）；删除残留的未使用字段/注释。
- 分析（原版睡眠跳过夜晚 vs 扎营扣耐久）：扎营扣耐久自 0.6.17 起位于每日扎营总结（`accumulateNightIllness`，按游戏日防重），不依赖睡眠时间窗口——MC 原版玩家睡觉把 `dayTime` 瞬间跳到次日 0 时，商队 AI 在新一天第一次 tick 仍会执行扣耐久（每天一次），机制生效；夜晚正常经过时同样生效。

## 0.6.29
- 诊断修复（状态转换日志不出现）：日志分析确认游戏日正常推进（215→217）、“扎营扣耐久”每天输出，但“日落/扎营/状态转换”日志从未出现——商队 AI 从未在夜晚（dayTime ≥ 日落/睡眠时间）时 tick（夜晚被加速/跳过导致窗口短于 AI 20 刻 tick 间隔）。现 `CaravanLogModule` 实现 `ITickingModule`，每殖民地刻直接按当前时间判定睡眠/夜行窗口，记录状态转换日志（带 dayTime 与总刻数）并在窗口边界变化时 `markDirty` 同步客户端——与商队 AI 是否运行无关。

## 0.6.28
- 回退：撤销 0.6.27 的全部改动（服务端同步 nightTravelNow / 客户端改用服务端判定），恢复 0.6.26 逻辑（客户端按本地时间推断“夜行中”，含无火把停止标记休息）。
- 诊断：新增服务端视角的地图状态转换日志（附带 dayTime 与总刻数）——`状态转换 X→Y（dayTime=A 总刻=B）`，覆盖 休息中↔旅行中、旅行中↔夜行中、夜行中→休息中，用于核对地图显示与实际状态。

## 0.6.27
- 修复（旅行地图“夜行中”误显示）：根因是客户端此前按**本地时间**自行推断“夜间”（`dayTime ≥ 日落`），而服务端商队是否夜行由“夜行窗口（日落 ~ 睡眠时间）”决定——当日落晚于睡眠时间（如夏季日落 12750 > 12600）时夜行窗口为空，商队从未夜行，但客户端在 12750 后仍显示“夜行中”。现服务端在日志模块同步“当前是否处于夜间赶路窗口”（`isNightTravelNow`），地图直接使用该状态；夜行窗口边界变化（日落/日出/扎营）时立即 `markDirty` 同步客户端。

## 0.6.26
- 修复（旅行地图误显示“夜行中”）：夜间无火把时商队停止前进（不推进模拟、不消耗火把），旧实现未标记休息状态，导致地图在夜间错误显示“夜行中”；现在无火把停止时同样标记 resting（露宿/扎营休息）并同步客户端。

## 0.6.25
- 修复（旅行地图状态刷新）：旅行地图标签重绘签名的旧实现只包含 phase/resting/位置/图标——不包含“睡眠时间窗口”与昼夜判定，导致“扎营中/露宿中”“夜行中”等标签变化时不触发刷新（“交易中”因 phase 变化正常）；现签名直接以标签内容参与计算。服务端在睡眠时间窗口边界变化时立即 markDirty，保证客户端 `sleepTimeNow` 及时更新。
- 通报：删除出发日持久化（tripStartDay）与归来通报中的“本次出行 X 天”部分（重进游戏导致天数失真），扎营总结改为显示世界游戏日。

## 0.6.24
- 修复（旅行地图“扎营中/露宿中”不显示）：根因是 resting 状态依赖商队 AI tick 到殖民地睡眠时间分支——玩家睡觉跳过夜晚（或时间被加速）时 AI 从未在睡眠窗口内 tick，且 resting 变化也未触发客户端同步。现服务端在日志模块同步“当前是否处于殖民地睡眠时间窗口”（按当前游戏时间直接判定，与白天长度/日落无关），旅行地图与日志页均以 `resting || 睡眠时间窗口` 显示“扎营中/露宿中”；resting 变化时立即 `markDirty` 同步客户端。

## 0.6.23
- 通报天数：商队出发日期持久化到市民 NBT（JobCaravanLeader.tripStartDay），玩家在商队外出时重进游戏不丢失出发日，返回通报按持久化的出发日计算出行天数。
- 日志整理：取消所有频率大于 2 次/游戏日的开发诊断日志——包括快递员取货保留判定（建筑/最低存量/交易模块）、备货诊断、出发条件诊断、行程检查、请求健康检查、扫描统计（开放请求/工匠接管）、商队解析方被询问、火把请求判定、帐篷差额/已足、食物差额、喂食/无菜单食物、时间诊断、追溯链失败/新下放跳过/工匠未接管原因、成果运送等待状态等；同步删除对应节流计数器字段。
- 保留日志：每日扎营总结/扣耐久、日落/夜行结束/扎营、创建请求/提取帐篷、请求链创建/完成/注销、回归通报等事件驱动与 ≤2 次/游戏日日志。

## 0.6.22
- 进食机制：饱食度低于 6 时开始进食，并连续食用【菜单】食物直到饱食度 ≥ 18 或商队物品栏无菜单食物（单次最多 32 个，防止异常循环）；每次进食 1 个，经 `ItemStackUtils.consumeFood` 增加饱食度。

## 0.6.21
- 进食阈值：按本体实际饥饿阈值改为饱食度低于 6 时才进食（饱食度上限为 20）；取消进食冷却与 `setJustAte` 抑制机制（进食节奏仅由阈值控制）。

## 0.6.20
- 修复（食物异常消耗）：根因是喂食阈值 30 大于 minecolonies 饱食度上限 20（`CitizenData.increaseSaturation` 内 `Math.min(20, …)`），导致“永远需要进食”，每殖民地刻喂 1 个（4 名成员 = 约 4 食物/殖民地刻），并伴随进食声音（`consumeFood` 内部 `finishUsingItem` 播放音效）。现与本体 `CitizenAI.shouldEat()` 一致：饱食度 ≥ 20 不再进食，并增加进食冷却（1200 刻）与 `setJustAte(true)` 抑制重复进食。
- 旅行地图：取消“返回中”显示——休息时有帐篷“扎营中”、无帐篷“露宿中”；交易阶段“交易中”；去程/回程移动时白天“旅行中”、夜间（日落之后，EclipticSeasons 动态日落/回退 12000）“夜行中”（中英文本地化）。

## 0.6.19
- 修复（夜行结束日志）：`wasInNightTravelWindow` 标志在“夜行结束”判断**之前**被更新为当前值，导致“夜行结束”永远读不到旧的 true、永不输出；已调整为先判断（使用旧值）再更新标志。日落/夜行结束/扎营日志现在按 日落 →（夜行消耗）→ 夜行结束 → 扎营 的时序正常输出。

## 0.6.18
- 修复（喂食）：改用 `ItemStackUtils.consumeFood(stack, citizen, null)`——该 API 内部先按 `FoodUtils.getFoodValue` 增加市民饱食度再消耗食物；旧实现用 `FoodUtils.consumeFoodStack` 只消耗栈、不增加 minecolonies 饱食度，导致每 20 刻反复喂食（饱食度不恢复）、“喂食物品显示空气”（传入的栈被消耗后再读物品名）、日志刷屏。
- 日志：喂食日志节流输出（每 100 殖民地刻一条），并在进食前保存食物名（避免显示“空气”）。

## 0.6.17
- 日志：取消“火把消耗，火把存量 A→B”逐次汇报（每殖民地刻一条刷屏），保留夜行开始（“日落，开始夜行”）与新增夜行结束（“夜行结束，商队火把存量 Z”）日志。
- 修复（帐篷耐久）：扎营扣耐久改为【每天一次，与睡眠时间窗口解耦】——夜晚时间被跳过/加速导致 `isColonySleepTime` 窗口未被 AI 观察到时，耐久仍按游戏日正常扣除；睡眠时间分支仅保留停止移动与休息状态。
- 诊断：新增节流输出的时间判定日志（`dayTime / 日落 / 夜行窗口 / 睡眠时间 / 此前标志`），用于定位“夜行结束不再汇报、耐久不减少”的时间窗口问题。
- 修复（食物）：不再使用 `FoodUtils.getBestFoodForCitizen`（其内部 `canEat` 会因市民无家（home==null）或家等级过高（canEatLevel）拒绝随身菜单食物，导致“背包有 128 个菜单食物却不喂食”刷屏）；改为直接遍历商队物品栏匹配【菜单】选中食物进食。

## 0.6.16
- 修复（火把）：夜间赶路窗口（日落 ~ 殖民地工作时间结束前）内【无论有无帐篷】都夜行，每 1 殖民地刻消耗 1 火把（原实现错误地要求无帐篷才夜行，导致有帐篷时火把永不消耗）。
- 诊断（火把）：日落时输出“日落，商队火把存量 X，开始夜行”；进入殖民地睡眠时间输出“扎营，商队火把存量 Y”；每次夜行消耗输出“火把消耗，火把存量 A→B”；备货阶段节流输出火把请求判定依据（目标/背包/小屋/差额/请求状态）。
- 修复（帐篷耐久）：扎营扣耐久由 `sleeping` 标志改为【游戏日防重】（每日只扣一次），防止标志卡住后耐久不再下降；扣减时输出“扎营扣耐久 耐久 X→Y（人数 N，睡眠中，睡眠时间）”诊断；未找到帐篷时输出诊断。
- 诊断（食物）：喂食逻辑增加节流诊断——菜单未选择食物、每人饱食度检查、喂食动作（饱食度前后）、商队物品栏无菜单食物。
- 修复（火把请求被取消）：关闭商队小屋 GUI 时不再取消【消耗品请求】（商队帐篷/火把）——原逻辑取消所有 Stack 请求，导致火把请求被取消后商队外出期间永不重建、仓库火把不运送。

## 0.6.15
- 经验：分析并参照 minecolonies 快递员（`EntityAIWorkDeliveryman` 每次送达动作 `addExperience(1.5)`，经由 `CitizenExperienceHandler` 按主/副属性比例分配）——商队每次交易段结算时，领袖与每名健康商队成员按完成交易笔数获得 `1.5 × 笔数` 经验，由本 mod 的 `CaravanExperienceHandler` 按自定义属性比例（敏捷 100%/智力 50% 等）分配。
- 火把：夜间赶路（日落后 ~ 殖民地工作时间结束前）每 1 殖民地刻消耗 1 火把（原为每行进 20 格 1 个），无火把时停止前进。
- 食物核查：确认 AWAY 状态下——(1) 饱食度由 minecolonies 市民 tick 正常结算（持续下降）；(2) 本体进食 AI 因 AWAY 状态 `isOkayToEat=false`（`AbstractAISkeleton.canBeInterrupted`）不会打断，由本 mod `feedCaravanFromSupplies` 接管（每 20 刻、饱食度 < 30 时从商队物品栏吃菜单食物，`FoodUtils.consumeFoodStack` 走 MC 原版进食逻辑 `finishUsingItem` 增加饱食度）。

## 0.6.14
- 出行条件：进入出发等待阶段要求同时满足【帐篷 + 食物 + 火把 + 至少一项交易售出品】；帐篷/食物/火把各自在设置携带量 ≥ 1 时生效（携带量为 0 的项恒满足），均为硬性条件（硬上限只豁免售出品不足）。
- 食物备货：仿照帐篷/火把——只检查【菜单】页选中的食物，按设置携带量（组数 × 64）从小屋存储差额提取到领袖/成员背包（优先领袖、放不下转成员、仍放不下放回小屋），不创建请求（菜单模块最低存量自动补货）。
- 出发硬校验：出发瞬间校验背包中帐篷/食物/火把是否真正携带（携带量 > 0 的项），不足则回到备货。

## 0.6.13
- 修复：帐篷耐久未正确消耗——根因是扎营扣耐久直接修改 ItemStack 不触发市民数据持久化，实体卸载/重载后耐久回退；现在扣耐久/消耗火把/食用食物后均标记背包 dirty。
- 兼容：新增 EpicColonies 兼容 mixin——其 `CitizenMeshCache.resolveJob` 把自定义职业完整名拼到 minecolonies 命名空间导致 `ResourceLocationException: minecolonies:caravan:caravan_leader` 刷屏；mixin 重定向该拼接（路径含冒号按完整名解析），`@Pseudo` 保证未安装 EpicColonies 时静默跳过。
- GUI：【日志】-消耗品缩为单行——帐篷（最多 4，每顶独立图标含耐久条）、食物（最多 3）、火把（最多 3）按顺序排布，每个堆叠一个图标带数量角标。

## 0.6.12
- GUI：【日志】-消耗品模块新增食物与火把两行——食物/火把每个堆叠一个独立图标（带数量角标），帐篷行保留每顶独立图标（含耐久度条）；交易列表高度相应微调。
- 日志：每日扎营总结的“第 X 天”改为从商队出发开始计算（出发日为第 0 天），并增加“[商队Y]”前缀——多个商队同时出发时按商队小屋建立顺序编号（商队1…商队N）。
- 通报：回归通报增加商队编号与本次出行天数（本地化：中/英）。

## 0.6.11
- 火把（夜间赶路窗口调整）：核对 EclipticSeasons-0.14.0 后确认其支持按季节动态修改白天长度（`DynamicDaylightDuration` + 各节气 `DayTimes`，API 为 `EclipticUtil.getNightTime = 6000 + 白天长度/2`，夏季日落更晚、冬季更早）。
- 机制：夜间赶路窗口 = 日落时刻（EclipticSeasons 动态日落，未安装时回退原版 12000）~ 殖民地工作时间结束（原扎营时间，默认 12600 + 研究加成）之间；窗口内无帐篷时野营赶路（每 20 格消耗 1 火把，无火把停止前进）。
- 机制：殖民地工作时间结束后（无论有无帐篷）商队停止移动——有帐篷扎营休息并扣除帐篷耐久，无帐篷野营休息（不扣耐久）；工作时间恢复后继续模拟。

## 0.6.10
- 标签页：参照【下界矿井】新增【菜单】页（复用本体 `RestaurantMenuModule`/`RestaurantMenuModuleView`，位于【选区工具】与【设置】之间）——玩家选择商队食用食物，按【设置】页“食物携带组数”作为最低存量保留并自动补货。
- 设置页：绿宝石模块文字与输入框间距缩短（与帐篷模块一致）；新增“食物携带组数”“火把携带组数”模块（0..32，默认 2）。
- 消耗品：帐篷检查同时检查食物/火把差额——食物只统计（由菜单模块最低存量补货），火把按差额从小屋提取并创建建筑差额请求。
- 火把：AWAY 模拟旅行中，无帐篷野营时夜晚继续赶路，每行进 20 格消耗 1 火把；物品栏无火把则停止前进（有帐篷时夜晚照常扎营休息）。
- 食物：商队人员饱食度低于 30（本体 `GET_YOURSELF_SATURATION`）时，从商队任意物品栏食用菜单选定食物（接入本体 `FoodUtils` 进食）。
- 日志：无帐篷扎营条目改为每日扎营总结——当天消耗食物量、火把量、有/无帐篷（有帐篷记录帐篷耐久；无帐篷记录当前累计患病几率）；取消 debug 中的帐篷耐久度播报。
- 通报：商队回归时向殖民地玩家通报“回到殖民地，共售出 XX 物品，获得 XX 物品，消耗 A 火把，B 食物，C 帐篷，D 人因无帐篷野营而患病”（中英文本地化）。

## 0.6.9
- 机制：取消“携带量为 0 时不累积患病概率”的早退——玩家选择不携带帐篷时，无帐篷过夜仍正常每夜每人 +5% 累积患病概率（携带量 0 只允许无帐篷出发，不豁免疾病风险）。

## 0.6.8
- 设置：商队帐篷携带量可选范围扩展为 0..32；设为 0 时允许商队无帐篷备货/出行（帐篷条件恒满足），且无帐篷过夜不再累积患病概率（视为玩家主动选择不带帐篷）。
- 机制：恢复扎营时的帐篷耐久损耗（按商队人数实际扣除，耐久归零帐篷消失），仅取消【出发时】的耐久损耗（出发时仍保留 debug 播报）。

## 0.6.7
- 机制：取消出行/扎营时帐篷耐久度实际损耗（debug 播报“帐篷目前耐久度 X/60”保留）。
- GUI：【日志】-消耗品模块改为每顶帐篷一个独立图标（各含耐久度条，最多 9 顶），不再使用带数量角标的聚合图标。
- 机制（疾病）：阅读并核对 minecolonies 疾病系统（`CitizenDiseaseHandler.canBecomeSick`、`DiseasesListener.getRandomDisease`、`ICitizenData.getCitizenDiseaseHandler`）后新增——模拟旅行中无帐篷过夜时，每过一个游戏日每名商队人员累积 +5% 患病概率（已生病者跳过，上限 100%），返程回到小屋后统一按概率结算，患病者获得一种当前数据包可出现的随机疾病；患病概率随市民 NBT 持久化。
- 机制（疾病）：出行时生病的商队成员不参与商队——物品栏不计入商队物品栏（帐篷/交易品/消失传送均排除），成员 AI 不跟随领袖、留在殖民地。

## 0.6.6
- 核对：colonypathingedition 读心显示的“正在等待所需物品”对应 minecolonies 基类状态 `AIWorkerState.NEEDS_ITEM`（市民存在待处理的公民请求时进入）。
- 修复：帐篷请求由【公民请求】改为【建筑请求】（requester = 小屋）——此前使用 `checkIfRequestForItemExistOrCreate` 创建公民请求，市民会因待处理请求进入 NEEDS_ITEM 状态，本 mod AI 停止执行，导致无法继续提取/重建帐篷请求；改为建筑请求后快递员把帐篷送到小屋存储，AI 持续运行并按需提取。
- 修复：领取帐篷后立即按最新差额重新检查——差额仍存在时按 1..差额 弹性重建请求（专用令牌防重，请求结束后允许重建；已满足时自动取消残留请求），不再受基类“已完成请求不再创建”防重影响。

## 0.6.5
- 修复：帐篷检查不再局限于【备货】阶段——空闲/等待物品（WANDER）时同样执行帐篷检查（提取小屋帐篷/创建请求），避免“没有帐篷 → 无可用交易 → 不进入备货 → 永不请求帐篷”的死锁，无需再靠解雇-重新雇佣重置。
- 修复：备货阶段始终为行程中所有未满足交易创建售出品请求，不再因“任一交易已满足”而跳过——重复交易满足后，绿宝石补缺交易的售出品请求（如纸/黏土球）仍会创建（原日志表现为 `[8x1红砖][0x10绿宝石]` 只请求红砖）。
- 修复：移除备货循环中“任一交易满足即取消其它缺失请求”的逻辑（该逻辑会取消行程中仍需要的售出品请求并与重新创建形成死循环），仅保留取消“行程不再需要”与“已满足”请求。
- GUI：【设置】页帐篷数量文本改为“商队携带的帐篷数量：”，输入框样式与绿宝石最低存量输入框一致（60×18、同色、同列对齐）。

## 0.6.4
- 修复：【设置】页帐篷携带量输入框未渲染——BlockUI 不支持 `<textfield>` 元素，改用 `<input>`（与绿宝石最低存量输入框一致）。
- GUI：【日志】页【消耗品】模块下移回原位（标题 190/图标 203/文字 206），提示文字高度 +12（10→22，支持换行显示），交易列表高度恢复 122。
- 诊断增强：帐篷相关日志现在区分“背包已足（差额 0，不提取）”与“从小屋差额提取”，出发条件诊断输出背包/小屋/目标帐篷数量明细，便于定位“小屋帐篷未被提取”的问题。

## 0.6.3
### 商队帐篷相关修复
- 修复：商队领袖从小屋存储提取帐篷的逻辑——任意耐久度的帐篷均可提取，优先放入领袖背包，放不下转入商队成员背包；出发前强制校验帐篷已装入背包，杜绝“小屋有帐篷但无帐篷出发”；出发前提取交易品时一并按差额提取帐篷。
- 修复：帐篷携带量设置迁移到【设置】页面（与绿宝石最低存量同页，文本框输入，关闭/切换页面自动保存）；【选区工具】页面不再包含该输入框。
- 修复：删除不再使用的 CaravanTentCountMessage，帐篷携带量随 CaravanStockMessage 一并下发。
- GUI：【日志】页面【消耗品】模块标题取消隐藏（始终显示“消耗品：”），整个模块上移一行（11 像素），交易列表高度相应缩短。

## 0.5.0（里程碑）

### 请求系统接入（0.4.x 系列成果汇总）
- 商队小屋作为请求解析方（优先级 75）接入 minecolonies 请求系统：
  - 请求 → 商队小屋 → M（商队交易）→ S（售出物）→ 交易 → 成果运送 → 主请求注销；
  - 售出物请求按“缺口 = 需求 − 已有量”精确计算（弹性下限=单次成本、上限=缺口），
    支持仓库部分送达后分批交易（按已有量动态计算副本数），大订单（如 64 书架）可
    反复执行“售出物换绿宝石 → 绿宝石买书架”直到完成；
  - 已进入完成流程的请求不再重复接入/规划交易，杜绝悬空 M/S 与重复交易；
  - 成果运送（Delivery）由“商队小屋回调 + 轮询”双通道驱动主请求注销。
- 请求取消：级联取消请求链（S/M），解除父子关系避免失效 token 残留导致的
  NPE 与“无法手动取消”问题；主请求状态异常时自动清理悬挂记录。
- 最低绿宝石存量：建筑级强制保留（`getRequiredItemsAndAmount` 覆写 + 模块双保险），
  快递员取货不再收走最低存量。
- 工匠接管：扫描玩家/重试解析方的新下放请求（下放时刻检测）→ 追溯父链找到
  被工匠接单但缺材料的主请求 → 取消工匠配方/原料链 → 商队接管并创建自己的
  M/S 链；每殖民地刻清理主请求 children 中的非商队残留子请求，保证完成流程
  不被工匠重新接单干扰。
- 重复交易优化：请求期间仅取消与请求无关的重复交易，产出请求售出物的
  重复交易（如纸→绿宝石）继续执行。

### 商队 AI 与交易（早期成果）
- 商队领袖/成员职业、商队小屋建筑（5 级）、商队标记物品、村民交易记录与
  模拟经验/升级同步；
- 交易列表 GUI（村民/交易选择、数量、总览排序、日志、建筑统计、最低存量）、
  设置页（绿宝石最低存量）、标签页图标与本地化（en_US/zh_CN）；
- 消失模拟（去程/交易中/回程、穿越殖民地、多段目的地）、旅行地图联动、
  归来回库/备货/上午出发机制、敏捷速度/智力交易延迟属性、商队成员扩展背包；
- 交易经验授予村民并模拟未加载村民的升级与新增交易。
